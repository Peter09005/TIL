

~~~java 
ApplicationConfig ac = new AnnotationApplicationConfig(AppConfig.class)

/*
AppConfig.class 안에 들어있는 객체정보들을 찾아 객체를 생성해준다

여기서 AppConfig 위에 @Configuration을 붙이면 싱글톤을 보장해준다. -> 프록시 활용 

AppConfig.class 자체로도 @Bean 으로 컨테이너 안에 저장된다. 
*/



 
